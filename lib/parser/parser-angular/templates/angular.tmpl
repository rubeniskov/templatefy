var {%module.var%};

try {
  {%module.var%} = angular.module('{%module.name%}');
} catch (e) {
  {%module.var%} = angular.module('{%module.name%}', ['{%module.deps%}']);
}

{%moduleVar%}
  .run(['{%module.stargs%}', function({%module.args%}) {
      %s
  }]);
